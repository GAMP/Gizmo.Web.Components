@namespace Gizmo.Web.Components
@inherits CustomDOMComponentBase

<div class="@Class">
    <CascadingValue Value="@this" IsFixed="true">
        <ul class="gizmo-tab__ul">
            @foreach (var item in _items)
            {
                <li class="gizmo-tab__item">
                    <a class="gizmo-tab__link @GetTabItemClass(item)"  @onclick=@( e => ActivateItem(item, e, false))>@item.Header</a>
                </li>
            }
        </ul>
        <div class="gizmo-tab-content">
            @foreach (var item in _items)
            {
                <div class="@GetTabItemContentClass(item)">
                    @item.ChildContent
                </div>
            }
        </div>
        @ChildContent
    </CascadingValue>
</div>